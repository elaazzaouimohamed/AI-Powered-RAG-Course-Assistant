# ── Extraction PDF ────────────────────────────────────────────────────────────
pymupdf>=1.23.0          # fitz : extraction de texte natif, rendu PNG
pytesseract>=0.3.10      # wrapper Python pour Tesseract OCR
Pillow>=10.0.0           # traitement d'images (conversion pixmap → PIL)
pdf2image>=1.16.3        # alternative poppler pour le rendu (optionnel)

# ── Chunking ─────────────────────────────────────────────────────────────────
tiktoken>=0.6.0          # comptage de tokens compatible avec les LLMs courants

# ── Embeddings & Retrieval ────────────────────────────────────────────────────
sentence-transformers>=3.0.0  # embeddings multilingues (paraphrase-multilingual-mpnet)
chromadb>=0.5.0               # base vectorielle locale persistante (remplace faiss)
rank-bm25>=0.2.2              # recherche sparse BM25 pour la recherche hybride
numpy>=1.26.0                 # calculs vectoriels
# ollama>=0.2.0               # optionnel : LLM local via Ollama
# faiss-cpu>=1.8.0            # optionnel : alternative à chromadb

# ── Interface ─────────────────────────────────────────────────────────────────
streamlit>=1.36.0        # interface web locale

# ── Configuration ─────────────────────────────────────────────────────────────
PyYAML>=6.0              # lecture de config.yaml
python-dotenv>=1.0.0     # chargement automatique du fichier .env

# ── API Claude (optionnel — active le fallback vision) ────────────────────────
anthropic>=0.30.0        # SDK officiel Anthropic (Claude)

# ── API NVIDIA (structuration JSON via Kimi K2) ───────────────────────────────
requests>=2.31.0         # appels HTTP vers l'API NVIDIA

# ── Développement / Tests ──────────────────────────────────────────────────────
pytest>=8.0.0
pytest-cov>=5.0.0
